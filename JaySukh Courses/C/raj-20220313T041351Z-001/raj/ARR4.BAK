//to find the maximum and minimum  no from array
void main()
{
	int a[10],i;
	int max;
	int min;
	clrscr();
	printf("Enter any 10 int values");
	for(i=0;i<10;i++)
	{
		printf("Enter value");
		scanf("%d",&a[i]);

	}
	max=a[0];
	min=a[0];
	for(i=1;i<10;i++)
	{
		if(max<a[i])
			max=a[i];
		if(min>a[i])
			min=a[i];
	}
	printf("The maximum is %d\n",max);
	printf("The minimum is %d",min);
	getch();
}

