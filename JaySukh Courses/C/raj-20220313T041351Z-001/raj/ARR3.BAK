//to count the no of odd and no of even no from array
void main()
{
	int a[10],i,ceven=0,codd=0;
	clrscr();
	printf("Enter any 10 int values");
	for(i=0;i<10;i++)
	{
		printf("Enter value");
		scanf("%d",&a[i]);
		if(a[i]%2==0)
		{
			ceven++;

		}
		else
		{
			codd++;

		}

	}
	printf("No of odd values are %d\n",codd);
	printf("No of even values are %d\n",ceven);
	getch();
}