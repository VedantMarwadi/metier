//to find the simple interest
#include<stdio.h>  //it is pre processor directive
#include<conio.h>  //to incloude any file

void main()
{
	double amt,rate,duration,i,famt;
	clrscr();
	printf("Enter amount rate and duration  respectively");
	scanf("%lf%lf%lf",&amt,&rate,&duration);
	i=(amt*rate*duration)/100;
	famt=amt+i;
	printf("Amount = %.2lf\nRate = %.2lf\nDuration = %.2lf\n",amt,rate,duration);
	printf("Interest = %.2lf\nfinal amount = %.2lf",i,famt);

	getch();

}